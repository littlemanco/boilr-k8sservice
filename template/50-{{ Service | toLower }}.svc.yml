---
# Generated by boilr at {{ time "Mon, 02 Jan 2006 15:04:05 MST" }}
kind: "Service"
apiVersion: "v1"
metadata:
  name: "{{ Service }}"
  namespace: "{{ Namespace }}"
spec:
  selector:
    application: "{{ Application }}"
    role: "{{ Role }}"
  ports:
    - protocol: "{{ ApplicationPortProtocol }}"
      name: "{{ ApplicationPortName }}"
      port: {{ ApplicationPort }}
    # - protocol: "{{ ApplicationPortProtocol }}"
    #   name: "another-{{ ApplicationPortName }}"
    #   port: 30{{ ApplicationPortName }}
  type: "{{ Type }}"
